import PropTypes from "prop-types";

const Nav = () => {
  return (
    <div className="tab">
      <button>Agregar Nueva tarea </button>
      <button>Ver las Tareas</button>
    </div>
  );
};

Nav.propTypes = {};

export default Nav;
